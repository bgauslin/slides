/** @enum {number} */
const ImageHeight = {
  SMALL: 400,
  MEDIUM: 600,
  LARGE: 800,
  PLACEHOLDER: 40,
};

/** @enum {number} */
const ImageWidth = {
  SMALL: 600,
  MEDIUM: 900,
  LARGE: 1200,
  PLACEHOLDER: 60,
};

/** @enum {number} */
const PublicationWidth = {
  SMALL: 320,
  MEDIUM: 480,
  LARGE: 640,
  PLACEHOLDER: 60,
};

/** @const {number} */
const THUMB_SIZE = 160;

export { ImageHeight, ImageWidth, PublicationWidth, THUMB_SIZE };