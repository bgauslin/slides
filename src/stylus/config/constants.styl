@import 'functions'

// @const String
ANIMATION_TIMING = cubic-bezier(.1, .1, .25, .9)

// @type String
DARK_GREY = #222

// @type String
LIGHT_GREY = #ddd

// @type String
CAPTION_MAX_WIDTH = rem(400)

// @type String
CONTROLS_HEIGHT = rem(60)

// @type String
COLUMN_GAP = rem(32)

// @type String
IMAGE_GAP_BASE = rem(16)

// @type String
IMAGE_GAP_MEDIUM = rem(24)

// @type String
LINK_HEIGHT_COVER = rem(40)

// @type String
LINK_HEIGHT_PUBLICATION = rem(32)

// @type String
SIDEBAR_WIDTH = rem(256)

// @type String
SLIDE_MAX_WIDTH = rem(960)

// @type String
THUMB_GAP_MEDIUM = rem(8)

// @type Hash
Stacking = {
  App: {
    CONTROLS: 2
    HEADER: 3
    PROGRESS_BAR: 4
  }
  Image: {
    PLACEHOLDER: 1
    HI_RES: 2
    SPINNER: 3
  }
  Cover: {
    IMAGE: -1
    OVERLAY: 2
  }
}

// -----------------------------------------------------------------------------
// Calculated constants.

// @type String
CONTENT_MAX_WIDTH = SIDEBAR_WIDTH + COLUMN_GAP + SLIDE_MAX_WIDTH

// -----------------------------------------------------------------------------
// Breakpoints.

// @type Hash
Breakpoint = {
  SMALL: 'screen and (min-width: %s)' % rem(480)
  MEDIUM: 'screen and (min-width: %s)' % rem(720)
  LARGE: 'screen and (min-width: %s)' % rem(960)
}